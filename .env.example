# Server
PORT=3000
# Security-first default: only listen on localhost. If you need LAN access,
# set HOST=0.0.0.0 (use with caution).
HOST=127.0.0.1

# Basic protection against random internet hits (ngrok/cloudflare will forward to this)
WEBHOOK_RATE_LIMIT_WINDOW_MS=60000
WEBHOOK_RATE_LIMIT_MAX=60

# LivePix webhook protection (optional but recommended)
# If set, you must include this token when LivePix calls your webhook:
# - Header: x-webhook-secret: <token>
# - OR Query: ?token=<token>
WEBHOOK_SECRET=

# LivePix field mapping (optional). If your LivePix webhook uses a different JSON shape,
# set these to dot-paths (example: data.value, data.message, data.sender.name)
LIVEPIX_VALUE_PATH=
LIVEPIX_MESSAGE_PATH=
LIVEPIX_SENDER_PATH=
LIVEPIX_STATUS_PATH=

# Accepted payment statuses (comma-separated). If LIVEPIX_STATUS_PATH is set and the
# extracted status is NOT in this list, the event will be ignored.
LIVEPIX_ACCEPTED_STATUSES=paid,confirmed,approved,completed

# LivePix API (optional: used when webhook only sends event id/type)
# Option A: static access token (expires; you'll need to keep it updated)
LIVEPIX_ACCESS_TOKEN=
# Option B: client credentials (server will fetch/refresh tokens)
LIVEPIX_CLIENT_ID=
LIVEPIX_CLIENT_SECRET=
LIVEPIX_SCOPE=messages:read subscriptions:read
LIVEPIX_API_BASE_URL=https://api.livepix.gg
LIVEPIX_OAUTH_TOKEN_URL=https://oauth.livepix.gg/oauth2/token

# Minecraft RCON
RCON_HOST=127.0.0.1
RCON_PORT=25575
RCON_PASSWORD=

# Music
# drop: VIP interrupts and the current track is discarded
# resume: VIP interrupts and the current track is put back at the front of the queue
MUSIC_INTERRUPT_BEHAVIOR=drop

# Rules file
RULES_PATH=config/rules.json
